# Create a ggplot2 spatial theme consistent with the ESC style guide.

Create a ggplot2 spatial theme consistent with the ESC style guide.

## Usage

``` r
esc_theme_sf(scale = 0.6)
```

## Arguments

- scale:

  Adjust the scale of the chart. Default is 0.6
